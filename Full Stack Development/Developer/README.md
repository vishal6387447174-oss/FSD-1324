# Developer (Client)

This folder is the client workspace. Developer changes must be made on a feature branch and submitted as a pull request to `main`.

Do not push directly to `main`. The pull request must be reviewed and approved by the TeamLead/Boss before it is merged and committed to GitHub.

Suggested branch name:

```text
developer/<short-change-name>
```